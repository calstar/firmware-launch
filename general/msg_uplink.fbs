namespace Calstar;

/* This is over RS422 only so it can be larger */

enum UplinkType : byte { FCOn = 0, FCOff = 1, BlackPowderOn = 2, BlackPowderOff = 3 }
table UplinkMsg {
    Type : UplinkType;
    BP : [bool];
}

root_type UplinkMsg;