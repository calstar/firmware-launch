namespace Calstar;

/* This is over RS422 only so it can be larger */

enum UplinkType : byte { BlackPowderOn = 0, BlackPowderOff = 1 }
table UplinkMsg {
    Type : UplinkType;
    BP : [bool];
}

root_type UplinkMsg;